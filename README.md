## Setup
1. Install Python 3.11+
2. Install ffmpeg and add to PATH
3. python -m venv .venv && source .venv/bin/activate on Linux (or.venv\Scripts\activate on Windows)
4. pip install -r requirements.txt
5. Fill out data/openings.csv
6. python main.py
